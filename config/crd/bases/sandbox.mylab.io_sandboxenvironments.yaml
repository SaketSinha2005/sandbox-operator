---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.13.0
  name: sandboxenvironments.sandbox.mylab.io
spec:
  group: sandbox.mylab.io
  names:
    kind: SandboxEnvironment
    listKind: SandboxEnvironmentList
    plural: sandboxenvironments
    singular: sandboxenvironment
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: SandboxEnvironment is the Schema for the sandboxenvironments
          API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: SandboxEnvironmentSpec defines the desired state of SandboxEnvironment
            properties:
              network:
                properties:
                  enabled:
                    type: boolean
                type: object
              resources:
                properties:
                  limits:
                    properties:
                      cpu:
                        pattern: ^[0-9]+m?$
                        type: string
                      memory:
                        pattern: ^[0-9]+(Mi|Gi)$
                        type: string
                    type: object
                  requests:
                    properties:
                      cpu:
                        pattern: ^[0-9]+m?$
                        type: string
                      memory:
                        pattern: ^[0-9]+(Mi|Gi)$
                        type: string
                    required:
                    - cpu
                    - memory
                    type: object
                type: object
              runtime:
                description: Foo is an example field of SandboxEnvironment. Edit sandboxenvironment_types.go
                  to remove/update
                properties:
                  command:
                    items:
                      type: string
                    type: array
                  image:
                    minLength: 1
                    type: string
                  language:
                    enum:
                    - python
                    - cpp
                    - java
                    type: string
                required:
                - image
                - language
                type: object
              security:
                properties:
                  allowPrivilegeEscalation:
                    type: boolean
                  readOnlyRootFilesystem:
                    type: boolean
                  runAsNonRoot:
                    type: boolean
                type: object
              storage:
                properties:
                  mountPath:
                    minLength: 1
                    type: string
                  size:
                    pattern: ^[0-9]+Gi$
                    type: string
                required:
                - size
                type: object
              timeout:
                type: string
            required:
            - resources
            - runtime
            type: object
          status:
            description: SandboxEnvironmentStatus defines the observed state of SandboxEnvironment
            properties:
              message:
                type: string
              phase:
                description: 'INSERT ADDITIONAL STATUS FIELD - define observed state
                  of cluster Important: Run "make" to regenerate code after modifying
                  this file'
                type: string
              podIP:
                type: string
              podName:
                type: string
              ready:
                type: boolean
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
